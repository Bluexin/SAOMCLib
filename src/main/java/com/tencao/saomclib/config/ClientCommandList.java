package com.tencao.saomclib.config;

import com.tencao.saomclib.packets.ClientCommandSync;

/**
 * List of text commands in json format that's
 * then sent to clients and translated to buttons.
 * @see ClientCommandSync
 */
public class ClientCommandList {

}
